import { EmptyLayout } from '@/theme/EmptyLayout';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: '布局部分的校验与测验',
  description: '布局是重中之重，只有好的布局才能彰显一个 ui 的基本功',
};

export default EmptyLayout;
