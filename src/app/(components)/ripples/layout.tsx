import { EmptyLayout } from '@/theme/EmptyLayout';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: '背景涟漪部分',
  description: '这是原有同步的组件样式，可能会导致低网速下的页面渲染阻塞',
};

export default EmptyLayout;
