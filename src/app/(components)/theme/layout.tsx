import { EmptyLayout } from '@/theme/EmptyLayout';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: '主题部分',
  description: '关于主题的一些配置，像色值、尺寸、样式展示',
};

export default EmptyLayout;
